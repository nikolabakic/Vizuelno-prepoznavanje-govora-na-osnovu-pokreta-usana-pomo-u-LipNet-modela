[project]
name = "lipnet-serbian"
version = "0.1.0"
description = "Vizuelno prepoznavanje srpskog govora pomoću LipNet modela"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mediapipe==0.10.21",
]

[dependency-groups]
dev = [
    "ipykernel>=6.30.1",
]
